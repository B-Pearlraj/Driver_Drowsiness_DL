streamlit>=1.32
pandas>=2.0
numpy>=1.24
plotly>=5.18
pillow>=10.0

# Optional - only needed for the "Live Detection Demo" tab to run real
# model inference instead of preview mode. Comment out if you only need
# the reporting/analytics tabs.
tensorflow>=2.15
